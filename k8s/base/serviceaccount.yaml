# ServiceAccount for the BFF. Used for RBAC and workload identity.
# Annotate with cloud-provider-specific annotations for IAM integration.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: thesa-bff
  labels:
    app.kubernetes.io/name: thesa-bff
    app.kubernetes.io/part-of: thesa
